# design/ — HTML návrhy

Interaktivní dema z fáze specifikace (odkazy v `CURRENT_STATE.md`):

- Klient „Sklo a čas" (9 obrazovek) — artefakt, zdrojové HTML zkopírovat sem.
- Backstage konzole — dtto.
- Marketingový web (46) — dtto.

TODO: až budou HTML zdrojáky k dispozici, uložit je sem jako
`klient.html`, `backstage.html`, `web.html`.
